import { DocumentViewerState } from './document-viewer.state';
import { DocumentViewerAction, Init, BeginSign } from './document-viewer.action';

export function documentViewerReducer(state: DocumentViewerState, action: DocumentViewerAction): DocumentViewerState {

    return {};
}
