import { TemplatedTextPipe } from './templated-text.pipe';

describe('TemplatedTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TemplatedTextPipe();
    expect(pipe).toBeTruthy();
  });
});
